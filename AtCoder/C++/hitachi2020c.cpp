#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    vector<int> a(n-1), b(n-1), tree(n, -1);
    for (int i=0; i<n-1; i++) {
        cin >> a.at(i) >> b.at(i);
    }

    

}