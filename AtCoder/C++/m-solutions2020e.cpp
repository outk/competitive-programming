#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ll n;
    cin >> n;

    vector<ll> x(n), y(n), p(n);
    for (int i=0; i<n; i++) {
        cin >> x.at(i) >> y.at(i) >> p.at(i);
    }

    for (int i=0; i<n; i++) {
        for (int j=0; j<n; j++) {
            
        }
    }
}