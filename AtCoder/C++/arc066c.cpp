#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    vector<int> a(n);
    for (int i=0; i<n; i++) {
        cin >> a.at(i);
    }

    sorted(a.begin(), a.end())

    int ans = 1;

    if (n%2) {
        for (int i=0; i<n; i++) {
            if (i%2) {

            } else {
                if (a.at(i) != )
            }
        }
    }
}