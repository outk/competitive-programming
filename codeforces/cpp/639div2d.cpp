#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ll n, m;
    cin >> n >> m;

    ll ans = 0;

    vector<string> cells(n);
    for (int i=0; i<n; i++) {
        cin >> cells.at(i);
    }
    for (int i=0; i<n; i++) {
        for (int j=0; j<m; j++) {
            if (cells.at(i).at(j) == '#') {
                ok = 
                break;
            }
        }
    }
        


}